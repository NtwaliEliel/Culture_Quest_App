import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF8B4513);
  static const Color primaryDark = Color(0xFF654321);
  static const Color textPrimary = Color(0xFF161411);
  static const Color textSecondary = Color(0xFF666666);
  static const Color background = Colors.white;
  static const Color cardBackground = Colors.transparent;
  static const Color accent = Color(0xFFD2691E);

  // Gradient colors for hero section
  static const List<Color> heroGradient = [
    Color(0x4D000000), // 30% black
    Colors.transparent,
  ];
}
